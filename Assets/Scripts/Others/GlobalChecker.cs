public static class GlobalChecker
{
    public static bool IsFirstLoad = true; //TODO: tirar isso depois, não precisa, no lugar usar o CurrentToken;
    public static string CurrentToken = "";
    public static bool isBGMOn = true;
    public static bool isSFXOn = true;
}